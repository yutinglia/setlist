# Copy to .env and adjust. Do not commit a real .env.

# App environment: dev | prod | test
# dev enables /docs, loose CORS, and local management endpoints
APP_ENV=dev

# Dev defaults to false so starting/reloading the API never scrapes unexpectedly.
# Production defaults to true.
BACKGROUND_UPDATER_ENABLED=false

# Adds channel/create and channel/video refresh endpoints. Defaults true only in dev.
MANAGEMENT_API_ENABLED=true

# Comma-separated browser origins for CORS (prod only; ignored when APP_ENV=dev)
# Example: https://example.com,http://localhost:3000
# Leave empty if no browser frontend yet
# CORS_ORIGINS=

# Database (inside Compose / Dev Container, use DB_HOST=db)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vks_db
DB_USER=vks_db_user
DB_PASSWORD=vks_db_pwd

# Optional: full URL overrides DB_* above
# DATABASE_URL=postgresql+asyncpg://vks_db_user:vks_db_pwd@localhost:5432/vks_db

# Seconds between DataUpdater cycles (default: 10 when APP_ENV=dev, else 1800)
# Do NOT use a short interval while scraping is enabled — prefer 1800+ or stop uvicorn
# when testing scrapers manually.
# DATA_UPDATE_INTERVAL=1800

# Phase 2 — YouTube access-limit avoidance (Tier B)
UPDATE_MAX_COMMENT_SCRAPES=5
UPDATE_MAX_VIDEOS=20
# Per-video metadata requests during a manual safe refresh
UPDATE_MAX_METADATA_SCRAPES=10
# Paced full-catalog video backfill for newly added channels
# UPDATE_BACKFILL_PAGE_SIZE=20
# UPDATE_BACKFILL_CHANNELS_PER_CYCLE=1
UPDATE_SCRAPE_SLEEP_MIN=3
UPDATE_SCRAPE_SLEEP_MAX=8
UPDATE_YOUTUBE_COOLDOWN_SECONDS=3600
UPDATE_MAX_ANALYZE_ATTEMPTS=3
UPDATE_MAX_COMMENTS_PER_VIDEO=50

# yt-dlp sleeps for comment scrapes (raised vs channel/video list scrapers)
YTDLP_COMMENT_SLEEP_INTERVAL=2
YTDLP_COMMENT_MAX_SLEEP_INTERVAL=10

# Phase 5 — optional LLM setlist cleaning (off by default; regex is primary)
# LLM_CLEANING_ENABLED=false
# LLM_API_URL=https://api.openai.com/v1/chat/completions
# LLM_API_KEY=
# LLM_MODEL=gpt-4o-mini
# LLM_TIMEOUT_SECONDS=30
# LLM_MAX_CLEANING_ATTEMPTS=2
# LLM_MAX_INPUT_CHARS=20000
